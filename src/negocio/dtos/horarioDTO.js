export class HorarioDto {
    constructor({ _id, detalle }) {

 /*       if (typeof _id !== 'number') throw new Error('tipo invalido')
        if (!_id) throw new Error('parametro requerido')
        this._id = _id

        if (typeof detalle !== 'string') throw new Error('tipo invalido')
        if (!detalle) throw new Error('parametro requerido')
        this.detalle = detalle*/
        
    }
}