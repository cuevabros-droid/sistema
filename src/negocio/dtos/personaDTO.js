export class PersonaDto {
    constructor({ _id, apellido, nombre }) {

 /*       if (typeof _id !== 'string') throw new Error('tipo invalido')
        if (!_id) throw new Error('parametro requerido')
        this._id = _id

        if (typeof apellido !== 'string') throw new Error('tipo invalido')
        if (!apellido) throw new Error('parametro requerido')
        this.apellido = apellido

        if (typeof nombre !== 'string') throw new Error('tipo invalido')
        if (!nombre) throw new Error('parametro requerido')
        this.nombre = nombre*/
        
    }
}